import chatserver
import argparse
import pymysql

# parser = argparse.ArgumentParser(description="""XYZ Messenger - Private encrypted chat room
# service for company XYZ. this is more information ...""")

# parser.add_argument('--dbpassword', dest='sqlpassword', required=False,
#                     help='Enter password for backend database system-user.')

# parser.add_argument('--dbuser', dest='dbuser', default='user-select', required=False,
#                     help='database user')
#
# parser.add_argument('--dbaddress', dest='dbaddress', default='127.0.0.1', required=False,
#                     help='IP address of SQL database.')
#
# parser.add_argument('--database', metavar='NAME', dest='database', default='chatroom', required=False,
#                     help='Name of database.')
#
# parser.add_argument('--bind', dest='address', default=('127.0.0.1', 60000), type=tuple, required=False,
#                     help='IP address to bind chat room socket.')


